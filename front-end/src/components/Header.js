import React from "react";
import { connect } from "react-redux";
import "./css/header.css"

let Header = (props) => {
    return (
        <header className="">
        </header>
    );
};

export default Header;
